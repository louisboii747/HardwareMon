info:
  name: Gaming Overlay Capabilities
  type: http
  seq: 8
  tags:
    - gaming

http:
  method: GET
  url: "{{baseUrl}}/gaming/overlay/capabilities"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/gaming/overlay/capabilities"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
