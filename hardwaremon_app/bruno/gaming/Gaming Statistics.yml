info:
  name: Gaming Statistics
  type: http
  seq: 6
  tags:
    - gaming

http:
  method: GET
  url: "{{baseUrl}}/gaming/statistics"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/gaming/statistics"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
